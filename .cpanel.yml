---
deployment:
  tasks:
    - export DEPLOYPATH=/home8/salplast/prueba.salplast.net/
    - /bin/cp -R Repositories $DEPLOYPATH
    - /bin/cp -R img $DEPLOYPATH
    - /bin/cp -R js $DEPLOYPATH
    - /bin/cp -R libs $DEPLOYPATH
    - /bin/cp -R vue $DEPLOYPATH